<Project>
  <!-- Import parent test Directory.Build.props for common test packages and repo settings -->
  <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))" />
</Project>
